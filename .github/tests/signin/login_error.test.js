const { test, expect } = require('@playwright/test');

test('try logging in with wrong password', async ({ page }) => {

	//TODO
	//verifier que console.log rend bien "Erreur xx"

});

test('modify profile page information', async ({ page }) => {

	//TODO
	//TODO: verifier qu'en modifiant le password par exemple il faut continuer de respecter les normes

});

test('loggin again without having logged out', async ({ page }) => {
	
		//TODO
	
});