const { test, expect } = require('@playwright/test');

test('try signing up with unaccepted information', async ({ page }) => {

    //TODO
    //verifier que console.log rend bien "Erreur xx"

});

test('try signing up with already existing username', async ({ page }) => {

    //TODO

});